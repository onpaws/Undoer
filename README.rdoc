== README
This is a basic Rails app based on a simple Post model (title, body).
Changes to models can be undone/redone (check the flash messages).

The seed file will generate a few sample Posts.

* Ruby version
Tested on Ruby 2.1.2, Rails 4.1.6
