%nav.navbar.navbar-default{role: 'navigation'}
  // Brand and toggle get grouped for better mobile display
  %div.navbar-header
    %button.navbar-toggle{"data-target" => ".navbar-ex1-collapse", "data-toggle" => "collapse", type: "button"}
      %span.sr-only Toggle navigation
      %span.icon-bar
      %span.icon-bar
      %span.icon-bar
    = link_to 'Undoer', root_url, class: 'navbar-brand'

  // Collect the nav links, forms, and other content for toggling
  %div.collapse.navbar-collapse.navbar-ex1-collapse
    %ul.nav.navbar-nav
      %li{class: ('active' if current_page? posts_path)}
        = link_to posts_path do
          <span class="glyphicon glyphicon-list"></span> Posts
      %li{class: ('active' if current_page? posts_history_path)}
        = link_to posts_history_path do
          <span class="glyphicon glyphicon-calendar"></span> History
      %li.dropdown
        %a.dropdown-toggle{href: '#', 'data-toggle' => 'dropdown'}
          Utilities
          %b.caret
        %ul.dropdown-menu
          %li
            = link_to 'Something', '#'
          %li
            = link_to 'Cool Link Bro', '#'
          %li
            = link_to 'Something else', '#'
          %li.divider
          %li
            = link_to 'Yeah', '#'
        -#= form_tag root_path, method: "get", class: 'navbar-form navbar-left', role: 'search' do
        -#  %div.form-group
        -#    = text_field_tag :find, '', placeholder: 'Find tags', class: 'form-control'
        -#  = submit_tag 'Find', class: 'btn btn-default'
    %ul.nav.navbar-nav.navbar-right
      %li
        /= link_to 'Link', '#'%a{href: '#'}
      %li.dropdown
        %a.dropdown-toggle{href: '#', 'data-toggle' => 'dropdown'}
          -#<span class="glyphicon glyphicon-cog"></span>
          - if user_signed_in?
            = current_user.email
          - else
            Account
          %b.caret
        %ul.dropdown-menu
          %li
            = link_to 'My Profile', edit_user_registration_path(current_user)
          %li.divider
          %li
            = link_to 'Sign Out', destroy_user_session_path, :method => :delete


-#.navbar.navbar-default{role:"navigation"}
-#  %p.navbar-text
-#    = link_to('Undoer', '/', :class => 'navbar-brand')
-#
-#  - if user_signed_in?
-#    %ul.nav.navbar-nav.navbar-right
-#      %li.dropdown
-#        %a.navbar-text.dropdown-toggle{ href:'#', data: {toggle:'dropdown'}}
-#          = current_user.email
-#          %b.caret
-#        %ul.dropdown-menu
-#          %li
-#            = link_to('My Profile', user_path(id: current_user.id))
-#          %li
-#            = link_to('My Account', edit_user_path(current_user))
-#          %li.divider
-#          %li
-#            = link_to('Logout', destroy_user_session_path, :method => :delete)
-#  - else
-#    %p.navbar-text.pull-right
-#      = link_to('How It Works', '/', :class => 'navbar-link')
-#      = render 'layouts/auth_links'